Given an array nums of n integers and an integer target, find the sum of the three integers in nums such that the sum is closest to target.

//three sum closest

public class Main
{   
    
    static int threeSumClosest(int arr[], int target){
        int bestSum = arr[0]+arr[1]+arr[2];
        
        for(int i=0; i<arr.length-2; i++){
            int L = i+1;
            int R = arr.length-1;
            
            while(L<R){
                int currentSum = arr[i]+arr[L]+arr[R];
                
                if(Math.abs(target-currentSum) < Math.abs(target-bestSum)){
                    bestSum = currentSum;
                }
                
                if(currentSum < target){
                    L++;
                }
                else if(currentSum > target){
                    R--;
                }
                else{
                    return currentSum;
                }
            }
        }
    return bestSum;
    }
    
	public static void main(String[] args){
	    int[] arr = {1,1,2,3,5,7,9,11};
	    int target = 51;
	    
	    System.out.println(threeSumClosest(arr, target));
	}
}
